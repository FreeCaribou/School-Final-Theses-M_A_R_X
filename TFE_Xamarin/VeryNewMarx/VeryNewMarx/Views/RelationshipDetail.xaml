<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="{Binding Title}"
             x:Class="VeryNewMarx.Views.RelationshipDetail">
    <StackLayout>
        <StackLayout Spacing="20" Padding="15" Orientation="Horizontal">
            <Label Text="{Binding Relationship.Name}"/>
            <Label Text="{Binding Relationship.Name}"/>
        </StackLayout>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Text="{Binding Relationship.Name}"/>
            <Label Grid.Column="1" Text="{Binding Relationship.Name}"/>
            <Label Grid.Column="2" Text="{Binding Relationship.Name}"/>
            <Label Grid.Column="1" Grid.Row="1" Text="{Binding Relationship.Name}"/>
            <Label Grid.Column="2" Grid.Row="2" Text="{Binding Relationship.Name}"/>
        </Grid>
    </StackLayout>
</ContentPage>